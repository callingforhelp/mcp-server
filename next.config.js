/** @type {import('next').NextConfig} */
const nextConfig = {
  // This is the default behavior, but explicitly setting it might help Vercel
  // recognize the pages/api directory correctly.
  // For Next.js 13+, API routes are typically in `app/api` or `pages/api`.
  // Since we are using `pages/api`, no special configuration is usually needed here.
  // However, adding an empty config can sometimes trigger Vercel to re-evaluate.
};

module.exports = nextConfig;
